/**
 * Created by I.Denisovs on 16.25.5.
 */

var request = require('request').defaults({json: true});
var assert = require('chai').assert;
var host = require('../host.json').host;

function restApiTests()
{
    
}

module.exports = restApiTests;